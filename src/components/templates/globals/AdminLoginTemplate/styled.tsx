import { styled } from '@/styles'

export const LoginFormArea = styled.div`
  width: 100%;
  height: ${({ theme }) => theme.height.minContent};
  display: flex;
  justify-content: center;
  align-items: center;
`
